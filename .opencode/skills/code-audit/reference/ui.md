# UI Audit Criteria

## shadcn Components
- shadcn components used when possible (no hand-built UI components)?
- CRITICAL: ALWAYS use shadcn component when possible. DO NOT make UI components.
- Flag any hand-built buttons, inputs, cards, modals, dialogs, etc. that should be shadcn
